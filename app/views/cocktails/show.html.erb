<%= link_to "Home", cocktails_path, class: 'button' %>
<h1 class="writing"><%= @cocktail.name %></h1>

<% @cocktail.doses.each do |dose|%>
 <h5 ><%= dose.ingredient.name %> <%= dose.description %> &nbsp &nbsp &nbsp &nbsp<%= link_to "", dose_path(dose), method: :delete, class: "far fa-trash-alt"%></h5>
<% end %>
<%= link_to 'Add ingredients', new_cocktail_dose_path(@cocktail), class: 'button', id: 'modify' %>
